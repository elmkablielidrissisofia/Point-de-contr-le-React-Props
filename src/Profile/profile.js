import React from 'react';
import PropTypes from 'prop-types';

const Profile = (props) => {
    return ( <div>
        <p>{props.FullName}</p>
    </div> );
}
const Profile = (props) => {
    return ( <div>
        <p>{props.bio}</p>
    </div> );
}
const Profile = (props) => {
    return ( <div>
        <p>{props.profession}</p>
    </div> );
}

 
 
 
export default Profile;